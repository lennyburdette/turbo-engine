# https://taskfile.dev
version: "3"

vars:
  BINARY: envmanager
  CMD_DIR: ./cmd/envmanager
  PORT: '{{.PORT | default "8083"}}'

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list-all

  build:
    desc: Build the envmanager service binary
    cmds:
      - go build -o bin/{{.BINARY}} {{.CMD_DIR}}
    sources:
      - "**/*.go"
      - go.mod
      - go.sum
    generates:
      - bin/{{.BINARY}}

  test:
    desc: Run all tests with race detector
    cmds:
      - go test -race -count=1 -cover ./...

  lint:
    desc: Run golangci-lint
    cmds:
      - golangci-lint run ./...

  run:
    desc: Run the envmanager service locally
    cmds:
      - go run {{.CMD_DIR}}
    env:
      PORT: "{{.PORT}}"
      LOG_LEVEL: debug

  tidy:
    desc: Tidy go modules
    cmds:
      - go mod tidy

  clean:
    desc: Remove build artifacts
    cmds:
      - rm -rf bin/
