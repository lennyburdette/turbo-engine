name: federation
kind: graphql-supergraph
version: "1.0.0"

dependencies:
  - name: federation/users
    version: "^1.0.0"
  - name: federation/products
    version: "^1.0.0"
  - name: federation/reviews
    version: "^1.0.0"
  - name: federation/operations
    version: "^1.0.0"

metadata:
  title: "E-Commerce Supergraph"
  description: "Composed supergraph that federates Users, Products, and Reviews subgraphs."
  team: platform-examples
